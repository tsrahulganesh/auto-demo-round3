pipeline:
  name: auto3pipe
  identifier: auto3pipe
  projectIdentifier: Demo_gitrepo
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: rrahul
        repoName: auto-demo-round3
        build: <+input>
  stages:
    - stage:
        name: auto3build
        identifier: auto3build
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          buildIntelligence:
            enabled: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    shell: Bash
                    command: mvn clean install
                  description: desc
